interface:
  display_name: "Ingest AWS Cost Reports"
  short_description: "Reconcile Excelsior AWS cost data"
  default_prompt: "Use $ingest-aws-cost-reports to reconcile weekly reports, finalized invoices, and current Cost Explorer data."
